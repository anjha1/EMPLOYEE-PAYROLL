<!DOCTYPE html>
<html>
<head>
  <title>Edit Employee</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<header class="header"><div class="logo">EMPLOYEE PAYROLL</div></header>
<div class="form-container">
<form method="POST" action="/edit/<%= employee.id %>">
  <h2>Edit Employee</h2>
  <div class="form-group">
    <input type="text" name="name" value="<%= employee.name %>" required>
  </div>
  <div class="form-group">
    <select name="department" required>
      <option <%= employee.department.includes("HR") ? "selected" : "" %>>HR</option>
      <option <%= employee.department.includes("Sales") ? "selected" : "" %>>Sales</option>
      <option <%= employee.department.includes("Finance") ? "selected" : "" %>>Finance</option>
      <option <%= employee.department.includes("Engineer") ? "selected" : "" %>>Engineer</option>
    </select>
  </div>
  <div class="form-group">
    <input type="number" name="salary" value="<%= employee.salary %>" required>
  </div>
  <div class="buttons">
    <button type="submit">Update</button>
  </div>
</form>
</div>

</body>
</html>